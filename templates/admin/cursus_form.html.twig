{% extends 'base.html.twig' %}

{% block title %}Gérer un Cursus{% endblock %}

{% block body %}
    <h1>{% if cursus.id is defined %}Modifier{% else %}Ajouter{% endif %} un Cursus</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_content') }}">Retour</a>
{% endblock %}
